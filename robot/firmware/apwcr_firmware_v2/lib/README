This folder holds local third-party libraries that are compiled with the active firmware project.

Current contents include vendor/source snapshots used by the robot firmware, such as:

- Encoder support libraries
- HC-SR04 ultrasonic support libraries

Treat these as dependencies rather than project logic. Most robot behavior changes should happen in `src/` or `include/`, not here.

