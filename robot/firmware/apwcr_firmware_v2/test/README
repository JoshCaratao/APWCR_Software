This PlatformIO `test/` folder is reserved for firmware unit or integration tests.

The current project does not appear to rely on automated PlatformIO tests yet, so future teams can use this location if they want to add:

- serialization/protocol tests
- controller math tests
- hardware-abstraction tests with mocks

