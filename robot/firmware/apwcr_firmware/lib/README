Local library dependencies for the older firmware snapshot live here.

These files support the archived `apwcr_firmware` project and are kept for reproducibility/reference rather than as the main development target.

