Reserved PlatformIO test folder for the older firmware snapshot.

No active automated test suite is documented here today. Keep it available if a future team wants to add regression tests while comparing older and newer firmware behavior.

