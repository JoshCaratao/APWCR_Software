Local library dependencies for the feedforward characterization firmware live here.

This firmware is intentionally minimal and exists to support the Python sweep runner in `robot/python/feedforward_model_test/`.

