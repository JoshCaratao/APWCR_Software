Header files for the feedforward characterization firmware live here.

Important headers:

- `Pins.h`
  Pin assignments used during characterization runs.
- `Params.h`
  Serial settings and test-related constants used by the sweep firmware.

This project is separate from the deployed robot firmware and is intended only for motor characterization experiments.

